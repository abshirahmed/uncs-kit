{
  "name": "uncs-kit",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "setup": "cd packages/pull-all && bun link && cd ../atlassian-cli && bun link"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
